name: Release & Publish

# Changesets-driven release pipeline: push to main → version PR → publish → Linear sync.
#
# npm auth = OIDC trusted publishing (no token), bound to this repo + workflow filename
# (PR #52). KEEP this file named `publish.yaml` — renaming breaks publishing.

on:
  push:
    branches: [main]
  workflow_dispatch: # manual re-trigger if a Version-PR merge push needs re-running
  # Runs the Verify job as a pre-merge gate; `labeled` also drives opt-in previews.
  pull_request:
    types: [opened, synchronize, reopened, labeled]

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions: {} # each job declares only the permissions it needs

jobs:
  verify:
    name: Verify
    # Runs on push (release chain) and on PRs (pre-merge gate so toolchain/CI
    # changes are validated before they reach main).
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install dependencies
        uses: ./.github/actions/pnpm-install
      - name: Check code
        run: pnpm check
      - name: Check circular deps
        run: pnpm check:circular-deps
      - name: Check unused deps/files
        run: pnpm knip:check
      - name: Build
        run: pnpm build
      - name: Check types
        run: pnpm check:types
      - name: Test
        run: pnpm test

  changesets:
    name: Version
    needs: verify
    # push/dispatch only — never on PRs (a passing PR Verify must not trigger
    # a version bump) — and not on forks.
    if: github.event_name != 'pull_request' && github.repository == 'lifinance/bigmi'
    permissions:
      contents: write # to create the version PR
      pull-requests: write # to create the version PR
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      has-changesets: ${{ steps.changesets.outputs.has-changesets }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # full history so Changesets can generate changelogs with correct commits
          fetch-depth: 0
      - name: Install dependencies
        uses: ./.github/actions/pnpm-install
        with:
          cache: 'false' # privileged job — don't restore a potentially poisoned cache
      # v2 pushes the release commit via the GitHub API (signed with GitHub's GPG
      # key, attributed to the token owner) — `contents: write` above covers it.
      # `github-token` defaults to `${{ github.token }}`; v2 ignores the old
      # GITHUB_TOKEN env var, so it is deliberately not set here.
      - name: Create version PR
        id: changesets
        uses: changesets/action@ae32849d5ba541f9ae29e40e22a623bc13562f51 # v2.1.2
        with:
          pr-title: 'chore: version packages'
          commit-message: 'chore: version packages'
          version-script: pnpm changeset:version

  release:
    name: Publish
    needs: changesets
    # Only publish once the version PR is merged (no pending changesets remain).
    if: needs.changesets.outputs.has-changesets == 'false'
    permissions:
      contents: write # to create GitHub Releases (changesets/action)
      id-token: write # OIDC token for npm trusted publishing + provenance
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      published-packages: ${{ steps.changesets.outputs.published-packages }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Install dependencies
        uses: ./.github/actions/pnpm-install
        with:
          cache: 'false' # privileged job — don't restore a potentially poisoned cache
      # v2 reads published packages from the CHANGESETS_OUTPUT file (no more stdout
      # parsing), so that env var must reach the `changeset publish` call inside
      # `changeset:publish` — it does, via plain `pnpm run` env inheritance.
      - name: Publish to npm
        id: changesets
        uses: changesets/action@ae32849d5ba541f9ae29e40e22a623bc13562f51 # v2.1.2
        with:
          publish-script: pnpm changeset:publish
          create-github-releases: true
        env:
          NPM_CONFIG_PROVENANCE: true
  # Linear "Bigmi" sync, anchored on @bigmi/react (top of the graph, what consumers
  # install). core/client-only cycles (no react bump) are skipped — reconcile manually.
  linear:
    name: Linear Sync (Bigmi)
    needs: release
    if: contains(needs.release.outputs.published-packages, '"@bigmi/react"')
    permissions:
      contents: read
    uses: ./.github/workflows/linear-release.yaml
    with:
      release_name: Bigmi
      package_name: '@bigmi/react'
      published_packages: ${{ needs.release.outputs.published-packages }}
    secrets:
      access_key: ${{ secrets.LINEAR_RELEASE_ACCESS_KEY }}

  # Preview release (opt-in): the `release-preview` label publishes a throwaway
  # 0.0.0-preview-<sha> to the `preview` dist-tag. Gated by same-repo + Triage+-to-label
  # (forks can't trigger); secret-isolated; 0.0.0-preview can't become latest/beta.
  # NEVER convert this to pull_request_target.
  preview:
    name: Preview release (PR)
    if: >-
      github.event_name == 'pull_request'
      && github.event.label.name == 'release-preview'
      && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write # npm OIDC trusted publishing + provenance
      pull-requests: write # post the install comment + remove the label
    steps:
      - name: Checkout PR head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Install dependencies
        uses: ./.github/actions/pnpm-install
        with:
          cache: 'false' # privileged job — don't restore a potentially poisoned cache

      # Composite (not a reusable workflow) so npm OIDC stays bound to publish.yaml.
      - name: Publish preview
        uses: ./.github/actions/preview-publish
